# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
#	name = shingo
#	email = shingo@shingonoMacBook-Air.local

[alias]
    s = status
    ss = status -s
    co = checkout
    b = branch
    d = diff

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
